# 修正要件

下記に提示したUI Layout案から全体のシステム要件の修正を行うこと

- サイドバーのレイアウトを変更し下記の項目を配置
    - 初期情報
    - 疏通確認
    - Github Repository
- メインエリアのレイアウトを下記の3カラムに修正
    - 世界観定義
        - 入力情報の切り替え
            - テキスト入力
            - 文章ファイル選択（markdown、最大10ファイルまでの複数選択にするのが望ましい）
        - プレビュー表示
    - 変換対象
        - 入力情報の切り替え
            - テキスト入力
            - 文章ファイル選択（markdown、最大10ファイルまでの複数選択にするのが望ましい）
            - 画像ファイル選択
        - プレビュー表示
        - 拡張機能
            - 世界観の影響度
            - 時系列の進行度
    - 生成結果
        - プレビュー表示
- Markdownファイルの登録機能を追加
    - 追加対象
        - 世界観定義
        - 変換対象

```python
import streamlit as st
import markdown
import glob

# Page Config
st.set_page_config(
    page_title="Functor Engine",
    page_icon="🔮",
    layout="wide",
    initial_sidebar_state="expanded"
)

# --- Sidebar: World Management ---
st.sidebar.title("🔮 Functor Engine Info")

st.sidebar.markdown("---")
st.sidebar.subheader("初期情報")
st.sidebar.button("初期化")
st.sidebar.info("Initialized Status: ")

st.sidebar.markdown("---")
st.sidebar.subheader("疎通確認")
st.sidebar.button("実行")
st.sidebar.info("Categorize Test: ")
st.sidebar.info("Generated Test: ")

st.sidebar.markdown("---")
st.sidebar.subheader("Github Repository")
st.sidebar.button("Link")

# --- Main Area ---
st.title("🔮 Functor Engine")
st.markdown("---")

col1, col2, col3 = st.columns([1, 1, 1])

with col1:
    st.subheader("１．世界観を定義")
    
    col1tab1, col1tab2 = st.tabs(["text" ,"file"])
    with col1tab1:
        world_def_input = st.text_area(
        "Define your World (Text/Markdown)",
        height=200,
        placeholder="Example:\nIn the Kingdom of Aethelgard, magic flows from the Crystal Spire. The King rules with an iron fist, but the Rebellion of the Red Rose is growing..."
        )
    with col1tab2:
        uploaded_file = st.file_uploader("Markdownファイルをアップロード", type="md", key="category")
        if uploaded_file is not None:
            with io.open(file_path, mode="r", encoding="utf-8") as f:
                df = f.open(uploaded_file)
                st.write("データのプレビュー:")
                st.dataframe(df.head())

    st.info("File Status: ")

with col2:
    st.subheader("２．変換対象")
    col2tab1, col2tab2, col2tab3 = st.tabs(["text" ,"file" ,"image"])
    
    with col2tab1:
        input_text = st.text_area("Enter text to translate:", height=150, placeholder="I went to the convenience store to buy some rice balls.")
    with col2tab2:
        input_file = st.file_uploader("Markdownファイルをアップロード", type="md", key="input")
        if input_file is not None:
            with io.open(file_path, mode="r", encoding="utf-8") as f:
                df = f.open(input_file)
                st.write("データのプレビュー:")
                st.dataframe(df.head())
        st.info("File Status: ")
    with col2tab3:
        st.warning("実装予定")

    on = st.toggle("拡張機能", False, key="expand")
    if on:
        st.slider("世界観の影響度", 1, 100, 100, key="effort")
        st.slider("時系列の進行度", 1, 100, 1, key="timeline")

with col3:
    st.subheader("３．生成結果")
    if st.button("Translate to World"):
        if not input_text.strip():
            st.warning("Please enter some text.")
        else:
            with st.spinner("Consulting the Laws of the World..."):
                result = api.translate(input_text)
                
                if "error" in result:
                    st.error(f"Translation Failed: {result['error']}")
                else:
                    st.success("Translation Complete!")
                    st.markdown("### Result")
                    st.markdown(f"> {result['translated_text']}")
                    
                    with st.expander("See Applied Laws"):
                        for law in result.get('applied_laws', []):
                            st.text(law)    
    if st.button("Refresh Graph"):
        st.rerun()
```